# Zavisnosti i build
node_modules/
dist/
*.tsbuildinfo
coverage/

# Tajne / lokalna konfiguracija — NIKAD u repo
.env
.env.*
!.env.example

# Ostalo
*.log
.DS_Store
Thumbs.db

# Backup dumpovi — sadrže sve podatke klijenata i hešove lozinki. Nikad u git.
backups/
*.sql.gz

# Fotografije vozila — lični podaci klijenata. Nikad u git (štite se sinhronizacijom).
uploads/

# Lične beleške i tajne — NIKAD u repo
00 GMAIL.txt
*GMAIL*
*.secret
secrets.env
